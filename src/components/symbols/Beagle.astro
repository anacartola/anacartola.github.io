---
/** Beagle — didactic / tracking. Geometric silhouette placeholder.
 *  fill="currentColor". Replace with the final brand asset when available. */
interface Props { size?: number; title?: string; class?: string; }
const { size = 40, title, class: className } = Astro.props;
---
<svg width={size} height={size} viewBox="0 0 48 48" fill="currentColor"
  role={title ? "img" : undefined} aria-hidden={title ? undefined : "true"} class={className}>
  {title && <title>{title}</title>}
  <!-- ponytail: placeholder geometry — swap for final Beagle silhouette -->
  <path d="M10 20 L14 12 L18 20 L30 20 L34 12 L38 20 L38 30 L34 30 L34 36 L30 36 L30 30 L18 30 L18 36 L14 36 L14 30 L10 30 Z" />
  <circle cx="20" cy="24" r="1.3" fill="var(--bg)" />
  <circle cx="28" cy="24" r="1.3" fill="var(--bg)" />
</svg>
